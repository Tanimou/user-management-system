
# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/user_management_db"
DATABASE_URL_TEST="postgresql://username:password@localhost:5432/user_management_test"

# Production Database Configuration
# For production, use a connection pooler like PgBouncer or the direct URL
DIRECT_URL="${DATABASE_URL}"  # For migrations, often the same as DATABASE_URL

# JWT Configuration
JWT_SECRET="your-super-secret-jwt-key-change-in-production-min-256-bits"
JWT_ACCESS_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"


# JWT Configuration  
JWT_ACCESS_SECRET="[64-character random string]"
JWT_REFRESH_SECRET="[64-character random string]"
JWT_ACCESS_TTL_SECONDS="900"
JWT_REFRESH_TTL_SECONDS="604800"

# CORS Configuration
FRONTEND_ORIGIN="https://your-app.vercel.app"


# Production Database Seeding (Optional)
# ADMIN_DEFAULT_PASSWORD="YourSecureAdminPassword123!"
# CREATE_SAMPLE_USERS="false"  # Set to "true" only if needed in production
# UPDATE_ADMIN="false"  # Set to "true" to update admin password during seed

# Optional: Redis for rate limiting (Upstash recommended for Vercel)
# UPSTASH_REDIS_URL=""
# UPSTASH_REDIS_TOKEN=""


# Optional: Monitoring
SENTRY_DSN="..."
LOG_LEVEL="info"

# Development Configuration (for local development)
DATABASE_URL_TEST="postgresql://username:password@localhost:5432/user_management_test"
FRONTEND_URL="http://localhost:3000"
NODE_ENV="development"

# Production Vercel Environment Variables (Set in Vercel Dashboard)
# These should be set as environment variables in your Vercel project:

#
# REQUIRED FOR PRODUCTION:
# - DATABASE_URL (Production PostgreSQL connection string with pooling)
# - DIRECT_URL (Direct PostgreSQL connection for migrations, may be same as DATABASE_URL)
# - JWT_SECRET (Strong production secret, minimum 256 bits, use: openssl rand -base64 32)
# - FRONTEND_ORIGIN (Your production domain, e.g., https://yourdomain.com)
# - NODE_ENV=production
#
# OPTIONAL FOR PRODUCTION:
# - ADMIN_DEFAULT_PASSWORD (Secure admin password, change after first login)
# - VERCEL_TOKEN (For deployment automation)
# - VERCEL_ORG_ID (Your Vercel organization ID)
# - VERCEL_PROJECT_ID (Your Vercel project ID)
# - SENTRY_DSN (Error tracking)
# - UPSTASH_REDIS_URL (Rate limiting)
# - UPSTASH_REDIS_TOKEN (Rate limiting)